# AWS Lambda Environment Variables
# These are configured in template.yaml and can be updated via AWS Console

# ===================================
# BACKEND (AWS Lambda)
# ===================================
# Set these during SAM deployment or in Lambda console

APP_NAME=ClubHub API
APP_VERSION=1.0.0
DEBUG=False

# IMPORTANT: Generate a secure random string for JWT
# Example: openssl rand -hex 32
JWT_SECRET_KEY=your-super-secret-jwt-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# CORS - Update with your actual Amplify URL after frontend deployment
CORS_ORIGINS=https://main.d1234567890.amplifyapp.com
FRONTEND_URL=https://main.d1234567890.amplifyapp.com

# Storage Options
USE_LOCAL_STORAGE=true  # false to use S3
LOCAL_STORAGE_PATH=/tmp/uploads
LOCAL_STORAGE_URL_PREFIX=https://your-api-id.execute-api.us-east-1.amazonaws.com/uploads

# S3 Configuration (if USE_LOCAL_STORAGE=false)
AWS_REGION=us-east-1
S3_BUCKET_NAME=clubhub-photos-your-unique-id

# ===================================
# FRONTEND (AWS Amplify)
# ===================================
# Set in Amplify Console → Environment Variables

# API URL - Update with your Lambda API Gateway endpoint
VITE_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com

# ===================================
# NOTES
# ===================================
# Lambda Storage Options:
# 1. Local (/tmp): 512MB max, data lost between cold starts
#    - Good for: Development, testing, small files
#    - Free (included with Lambda)
#
# 2. S3: Unlimited, persistent
#    - Good for: Production, many photos
#    - Cost: ~$0.023/GB/month
#
# 3. EFS: Persistent, expensive
#    - Good for: SQLite database persistence
#    - Cost: ~$0.30/GB/month
#    - Requires VPC setup (more complex)

# For production with many photos: Use S3
# For MVP with free credits: Use /tmp (Lambda local storage)
